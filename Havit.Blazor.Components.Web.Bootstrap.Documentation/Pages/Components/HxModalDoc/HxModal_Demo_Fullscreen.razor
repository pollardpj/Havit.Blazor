<HxModal @ref="smModal" Fullscreen="ModalFullscreen.SmallDown" Title="Fullscreen below SM">
	<BodyTemplate>
		...
	</BodyTemplate>
</HxModal>
<HxModal @ref="mdModal" Fullscreen="ModalFullscreen.MediumDown" Title="Fullscreen below MD">
	<BodyTemplate>
		...
	</BodyTemplate>
</HxModal>
<HxModal @ref="lgModal" Fullscreen="ModalFullscreen.LargeDown" Title="Fullscreen below LG">
	<BodyTemplate>
		...
	</BodyTemplate>
</HxModal>
<HxModal @ref="xlModal" Fullscreen="ModalFullscreen.ExtraLargeDown"  Title="Fullscreen below XL">
	<BodyTemplate>
		...
	</BodyTemplate>
</HxModal>
<HxModal @ref="xxlModal" Fullscreen="ModalFullscreen.XxlDown" Title="Fullscreen below XXL">
	<BodyTemplate>
		...
	</BodyTemplate>
</HxModal>
<HxModal @ref="xl2Modal" Fullscreen="ModalFullscreen.ExtraLargeDown" Size="ModalSize.Large" Title="Fullscreen below XL (otherwise L)">
	<BodyTemplate>
		...
	</BodyTemplate>
</HxModal>

<HxButton OnClick="() => smModal.ShowAsync()" Color="ThemeColor.Primary" CssClass="me-1 mb-1">Fullscreen below SM</HxButton>
<HxButton OnClick="() => mdModal.ShowAsync()" Color="ThemeColor.Primary" CssClass="me-1 mb-1">Fullscreen below MD</HxButton>
<HxButton OnClick="() => lgModal.ShowAsync()" Color="ThemeColor.Primary" CssClass="me-1 mb-1">Fullscreen below LG</HxButton>
<HxButton OnClick="() => xlModal.ShowAsync()" Color="ThemeColor.Primary" CssClass="me-1 mb-1">Fullscreen below XL</HxButton>
<HxButton OnClick="() => xxlModal.ShowAsync()" Color="ThemeColor.Primary" CssClass="me-1 mb-1">Fullscreen below XXL</HxButton>
<HxButton OnClick="() => xl2Modal.ShowAsync()" Color="ThemeColor.Primary" CssClass="me-1 mb-1">Fullscreen below XL (otherwise L)</HxButton>

@code
{
	private HxModal smModal;
	private HxModal mdModal;
	private HxModal lgModal;
	private HxModal xlModal;
	private HxModal xxlModal;
	private HxModal xl2Modal;
}